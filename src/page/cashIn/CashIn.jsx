import { Helmet } from "react-helmet";

const CashIn = () => {
    return (
        <div>
             <Helmet>
                <title>Cash-In</title>
            </Helmet>
            thi is cash in
        </div>
    );
};

export default CashIn;
