import { Helmet } from "react-helmet";

const SendMoney = () => {
    return (
        <div>
             <Helmet>
                <title>Send-Money</title>
            </Helmet>
            this is send money
        </div>
    );
};

export default SendMoney;